# dsh-captain-call · GitHub 上线可行性检查报告

> 结论：**代码层面可直接上线；当前唯一阻断项是素材版权，替换后即可发布。**（按你的要求：暂不上线，文件留在本仓库。）

## 1. 检查项与结论

| 检查项 | 结论 | 说明 |
|---|---|---|
| DSH 插件形态 | ✅ 通过 | 标准 bundle 插件：`package.json`（`dsh.bundle.patch` + `dsh.client.platform=web`）+ `cordis.patch.yml` + 预构建 `lib/index.js`、`lib/client.js`；与 maid-atelier / dsh-agent-teams 同构 |
| 依赖 | ✅ 通过 | 仅 peerDependencies（`@deepseek-ai/cordis`、`@deepseek-ai/schemastery`），由 DSH profile 提供，无需携带重依赖；无构建步骤（纯 JS 交付） |
| 安装/卸载 | ✅ 通过 | `dsh plugin --profile web add <path>` 安装、`remove` 卸载；effect 销毁器回收全部 DOM/CSS/定时器/音频/麦克风流，卸载即复原 |
| 运行验证 | ⚠️ 待重启验证 | 配置合并已通过 `dsh --profile web --dump-config` 校验；运行时行为需重启 `dsh web` 后用「测试来电」按钮验证 |
| 通知数据源 | ✅ 通过 | 只读 `.agent-teams/*/team.json`（AgentTeams 官方磁盘真相），不动其状态文件，插件缺失时也不影响 AgentTeams |
| 隐私/权限 | ✅ 通过 | 麦克风每次申请前先弹确认框；无网络外呼；状态轮询仅同源 HTTP 路由 |
| 代码许可 | ✅ 通过 | MIT（`LICENSE`） |
| **素材版权** | ❌ **阻断项** | `assets/` 内 5 张角色图 + 1 段铃声均为第三方版权素材（详见 `NOTICE.md`），**不得随仓库公开发布**；发布前必须替换为自有/CC0/已授权素材，或在 README 改为"用户自备素材"分发模式 |
| npm 发布 | ✅ 可行（改造后） | `private: true` 需改为 false；`files` 字段已收窄到 lib/assets/文档；npm 发布前需把素材替换并补 `repository`/`author` 字段 |
| GitHub Actions 等 | 不适用 | 无构建流程，无需 CI |

## 2. 上线前必做清单（按顺序）

1. **替换素材**：删除 `assets/` 下第三方图片与 `ringtone.m4a`，改为占位图 + README"自备素材"说明（文件名清单保留）；
2. `package.json`：`private` 改为 `false`，补 `repository`、`author`、`keywords`；
3. 更新 `NOTICE.md` 署名链（换成自有素材后保留本文件以记录历史）；
4. 本地全流程回归：安装 → 重启 → 测试来电（铃声/TTS/接听/挂断）→ 麦克风授权弹窗 → 语音回复/打字回退 → 卸载复原；
5. `git init` + 首次 commit（feat: initial release），推送到 GitHub 仓库；
6. （可选）`npm publish` 发布到 npm（此时素材已合规）。

## 3. 已知限制（文档中已声明，不构成发布阻断）

- 语音识别（SpeechRecognition）仅 Chromium 系浏览器；其他浏览器自动降级打字回复；
- 浏览器自动播放策略可能拦截无手势的铃声/TTS，接听按钮可作为手势入口；
- 通知基于轮询（默认 4s），非毫秒级实时（对"队员完成任务"场景足够）；
- 依赖 AgentTeams 插件的磁盘格式 `team.json`，若其格式变化需同步适配（可读性兼容处理已在代码中：字段缺失不抛错）。
